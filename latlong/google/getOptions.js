function getOptions(){
    var options = {showTip: true,
		   showLine: true,
		   lineColor: '#800000',
		   lineWidth: 1,
		   lineWeight: 5,
		   enableScrollWheel: true,
		   //      mapType: 'styledMap', // needs to define my style
		   useMapTypeControl: true,
		  };
    return options;
}
